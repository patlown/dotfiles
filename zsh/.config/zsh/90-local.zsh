# Machine-specific and private overrides. These files are not owned by dotfiles.

[[ -f "$HOME/.config/dev/local.zsh" ]] && source "$HOME/.config/dev/local.zsh"
[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"
